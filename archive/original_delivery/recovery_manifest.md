# Recovery Manifest

- Recovered from git commit `14cbd7f` (`Add files via upload`).
- Original notebook file restored with its original delivery name.
- The dataset from that delivery survives in the current repo as `annual_household_survey_2019.csv` after a later rename.
- The Markdown companion below extracts the original theoretical explanations from the Spanish notebook so they are easy to read without opening the `.ipynb`.
